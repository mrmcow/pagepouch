/**
 * Default Open Graph / Twitter card image (`public/og-image.png`).
 * Keep width/height in sync with the actual file (e.g. `sips -g pixelWidth -g pixelHeight`).
 */
export const PUBLIC_OG_IMAGE = {
  url: '/og-image.png',
  width: 1731,
  height: 909,
  alt: 'PageStash — Save any page. Organize everything. Web clipping dashboard with Chrome, Firefox, and Tor.',
} as const
